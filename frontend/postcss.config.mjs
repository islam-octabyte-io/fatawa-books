/**
 * Tailwind v4 is configured in CSS, not JS — see the `@theme` block in
 * `app/globals.css`. This plugin is the entire build-tool surface.
 */
const config = {
  plugins: ['@tailwindcss/postcss'],
};

export default config;
