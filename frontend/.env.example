# Origin of the backend REST API, without a trailing slash or the `/api` prefix.
# Must match the backend's PORT (see ../backend/.env.example).
#
# Not NEXT_PUBLIC_: the API is only ever called from server components, so this
# value stays on the server and no CORS negotiation is involved.
API_URL=http://localhost:4004
